{
  "name": "ganga-root",
  "private": true,
  "version": "1.0.0",
  "engines": { "node": "20.x" },
  "scripts": {
    "build": "npm --prefix admin ci && npm --prefix admin run build && mkdir -p public/admin && xcopy admin\\dist public\\admin /E /I /Y >NUL 2>&1 || cp -r admin/dist/* public/admin/ && Copy-Item admin.html public/admin.html"
  }
}


